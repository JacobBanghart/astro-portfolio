resources:
  - ../../application
  - ./domains.yaml
patches:
  - path: replica_count.yaml
nameSuffix: -dev
transformers:
  - |-
    apiVersion: builtin
    kind: NamespaceTransformer
    metadata:
      name: notImportantHere
      namespace: portfolio-dev
    unsetOnly: true